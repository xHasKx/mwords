local/
node_modules/
dist/
.vite/
*.log
.DS_Store

# Android signing material — never commit. A leaked .jks lets anyone
# publish updates as you on the Play Store.
*.jks
*.keystore

# Local secrets file (act / GitHub Actions local runs).
.secrets
